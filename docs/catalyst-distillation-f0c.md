# Catalyst distillation F0c — Costas mechanical oracle handoff

> **Branch:** `users/brianhu/ideation`  
> **F0a source revision:** `89081c13037c2ffc87592f01df634d7ba944b4be`  
> **F0c aggregate:** `sha256:e0b029e833d5543877bc10625d3556d8b1b9ef402ae2a31502842cd50d7e5f72`  
> **State:** candidate for AgentExperiments EM/WITNESS disposition  
> **Authority:** none; this handoff does not authorize F0b import, runtime/provider execution, product acceptance, mutation, deployment, or cutover.

## Outcome

Costas now has a deterministic mechanical evidence layer for the nineteen-case F0a semantic corpus. The four committed family captures are regenerated by their existing-owner test harnesses, while the aggregate test closes the case map, binds every capture and capture source by SHA-256, enforces canonical bytes, and rejects independent expected-side and observed-side mutations.

The aggregate test runs the three Python family capture functions with corpus paths replaced by fail-closed sentinels. Any attempt to read corpus expectations while constructing an observation fails. The UI family is not executed from Python: its committed artifact and Vitest source are hash-bound, and the narrow Vitest receipt remains the execution evidence.

No production code, schema bytes, corpus bytes, package lock, AgentExperiments file, or family capture/source file was changed by MC5.

## Owned F0c changes

```text
tests/fixtures/catalyst_oracle/manifest.json
tests/agent/test_catalyst_oracle_fixture.py
tests/catalyst_capture/test_mechanical_oracle.py
docs/catalyst-distillation-f0c.md
```

The manifest's closed `mechanical_capture` object has exactly four keys: `schema`, `artifacts`, `sources`, and `cases`. Its case map has exactly nineteen entries, each with exactly `artifact`, `family`, `status`, and `residual`. Status and residual remain bound to the corpus availability and refusal fields.

## Content identity

### Oracle and captured artifacts

| Artifact | SHA-256 |
|---|---|
| `schema.json` | `c036a5b440e9887f380ee7e4460a405c1f122670cf8185c05892c3f1f9ff2ac5` |
| `corpus.json` | `f435a5143ea5b74d56f0cc92d9f00d06fb189a661d3430d5cfe632c68084eaa9` |
| `captured/session.json` | `1aaceb8c8d95f93ab52a138fd87f756435265235595effd329a867bf2c7e7ee6` |
| `captured/turn_tool.json` | `6bf8250f7612268c67714c6cafe354c33f54e32c00c4e4b6c11e48f524f1fefb` |
| `captured/control_runtime.json` | `d840bd3462c33c75796a054faf1d3b5657bdd38dc27d9065c225946b868d55ec` |
| `captured/ui.json` | `a776bdc709c7bb5ee3c10ca455cabbbcf3dbeb5ecdb20d7a34a817a4068bde69` |

### Capture and aggregate sources

| Source | SHA-256 |
|---|---|
| `tests/catalyst_capture/test_session_lineage_capture.py` | `6ca277967d946579147607403009a16f639b1aca85a130450fb311412fe99547` |
| `tests/catalyst_capture/test_turn_tool_capture.py` | `70bcb502150262fde387292b273ec1a8c89900361f64fafb5c60b168fda928c2` |
| `tests/catalyst_capture/test_control_runtime_capture.py` | `232eb64257270083a844410c413155e722841dfcef401ef03319df3fe84761f3` |
| `apps/desktop/src/catalyst/__tests__/mechanical-capture.test.ts` | `d87e1c0d63a7cc79cb181edc0fc079694b4cc694062db35ba02c05ab8068d7e5` |
| `tests/catalyst_capture/test_mechanical_oracle.py` | `59a307a6a32752e4e291a3d5624fae47e9c19b36180754d24eafc388c0ef8e6f` |

The aggregate is SHA-256 over canonical bytes of:

```json
{"artifacts": manifest.artifacts, "mechanical_capture": manifest.mechanical_capture, "sources": manifest.sources}
```

Canonicalization remains `utf8-nfc-sort-keys-compact-lf/1`. The resulting aggregate is:

```text
e0b029e833d5543877bc10625d3556d8b1b9ef402ae2a31502842cd50d7e5f72
```

## Closed case receipt

| Family | Cases | Evidence artifact |
|---|---:|---|
| session | 4 | `captured/session.json` |
| turn | 1 | `captured/turn_tool.json` |
| tool | 3 | `captured/turn_tool.json` |
| control | 3 | `captured/control_runtime.json` |
| runtime | 4 | `captured/control_runtime.json` |
| fault | 2 | `tests/catalyst_capture/test_mechanical_oracle.py` mutation falsifiers |
| UI | 2 | `captured/ui.json`, executed by the hash-bound Vitest source |
| **Total** | **19** | closed manifest map |

`session-resume-unavailable` is independently visible in both the SessionDB and control/runtime probes; its primary family artifact remains `captured/session.json`. The aggregate ignores the duplicate only when constructing the unique observation index.

The three unavailable cells are unchanged:

- `session-resume-unavailable` → `resume-unavailable`;
- `runtime-replay-unavailable` → `durable-replay-unavailable`;
- `runtime-close-proof-unavailable` → `durable-close-proof-unavailable`.

## Aggregate falsifiers

The focused aggregate test proves:

1. corpus IDs, manifest case receipts, and independently captured non-fault IDs are total and closed;
2. every committed capture and capture/aggregate source matches its manifest SHA-256;
3. schema, corpus, manifest, and all four captures are canonical NFC UTF-8 JSON with one LF;
4. mutating a corpus expected final state independently makes validation fail;
5. mutating a captured observed final state independently makes validation fail;
6. no event marked `partition=control` carries `text` in either corpus or captures;
7. Python family capture still reproduces committed bytes when every expected-corpus path raises on access;
8. UI execution remains solely in Vitest; Python checks only its immutable receipt and canonical artifact.

## Fresh verification

### Focused Python gate

```text
HERMES_PYTHON=/Users/mutilar/.hermes/venvs/costas-code/bin/python \
  scripts/run_tests.sh \
  tests/agent/test_catalyst_oracle_fixture.py \
  tests/catalyst_capture/test_session_lineage_capture.py \
  tests/catalyst_capture/test_turn_tool_capture.py \
  tests/catalyst_capture/test_control_runtime_capture.py \
  tests/catalyst_capture/test_mechanical_oracle.py -q

5 files, 12 tests passed, 0 failed
```

The first wrapper invocation without `HERMES_PYTHON` refused because this worktree has no local `.venv` or `venv`; rerunning through the documented shared interpreter override produced the fresh GREEN above.

### Ruff

```text
/Users/mutilar/.hermes/venvs/costas-code/bin/python -m ruff check \
  tests/agent/test_catalyst_oracle_fixture.py \
  tests/catalyst_capture/test_mechanical_oracle.py

All checks passed!
```

### Narrow UI gate

From `apps/desktop`:

```text
npx vitest run src/catalyst/__tests__/mechanical-capture.test.ts

1 test file passed; 3 tests passed
```

The Node experimental `localStorage` warning remains non-failing and did not alter the result.

## Residuals and HOLDs

- Exact provider-thread resume after backend restart remains unavailable.
- Durable notification cursor/replay remains unavailable.
- Durable exact-thread terminal proof from process close remains unavailable.
- UI background evidence proves renderer route, DOM focus, and pane invariants, not native-window focus.
- UI cancel evidence proves immediate presentation before interrupt-request resolution, not backend cleanup terminal proof.
- Fault-family evidence is aggregate mutation/fail-closed validation rather than a fifth standalone captured JSON.
- No live model/provider, real network, credential, production tool, subprocess, deployment, physical-device, performance, accessibility, energy, or product-acceptance claim is made.
- AgentExperiments immutable import, reducer parity, Store/UGUI/native shell work, runtime authority, and production cutover remain HOLD.

## Rollback

Rollback is limited to MC5-owned paths:

1. restore the prior F0a bytes of `tests/fixtures/catalyst_oracle/manifest.json` and `tests/agent/test_catalyst_oracle_fixture.py`;
2. remove `tests/catalyst_capture/test_mechanical_oracle.py`;
3. remove `docs/catalyst-distillation-f0c.md`.

Do not modify or delete `schema.json`, `corpus.json`, any family capture/source file, production code, `package-lock.json`, or AgentExperiments state during rollback.

## Requested disposition

AgentExperiments EM/WITNESS: bind the decision to aggregate `e0b029e833d5543877bc10625d3556d8b1b9ef402ae2a31502842cd50d7e5f72` and return exactly one of:

```text
ATTEST F0c-MECHANICAL
REVISE
HOLD
```

An attestation confirms only this bounded mechanical oracle receipt. It grants no runtime, mutation, provider, product-acceptance, deployment, or cutover authority.
